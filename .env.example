# CLEF Configuration Example
# Copy this file to .env and modify as needed

# Upstream npm registry URL
# Default: https://registry.npmjs.org
CLEF_UPSTREAM_REGISTRY=https://registry.npmjs.org

# Server host address
# Default: 127.0.0.1 (localhost only)
# Use 0.0.0.0 to bind to all interfaces
CLEF_HOST=127.0.0.1

# Server port
# Default: 8000
CLEF_PORT=8000

# Cache Configuration
# Enable/disable caching
# Default: true
CLEF_CACHE_ENABLED=true

# Data directory path
# Default: ./data
CLEF_CACHE_DIR=./data

# Cache TTL in hours
# Default: 24 (24 hours)
CLEF_CACHE_TTL_HOURS=24

# Logging level
# Options: error, warn, info, debug, trace
# Default: info
RUST_LOG=info
